import { FormatSongPipe } from './format-song.pipe';

describe('FormatSongPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatSongPipe();
    expect(pipe).toBeTruthy();
  });
});
