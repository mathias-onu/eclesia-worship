import { FormatPlaylistPipe } from './format-playlist.pipe';

describe('FormatPlaylistPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPlaylistPipe();
    expect(pipe).toBeTruthy();
  });
});
